# Local data / 本地数据

Place competition files in `data/raw/` locally:
- `chars_final_with_names.parquet`
- `8k_20150101_20260831_identified.parquet`
- `factor_char_list.csv` (obtain the official whitelist; do not invent values)

`processed/` and `embeddings/` hold regenerated local artifacts. Only `.gitkeep` placeholders are versioned. Share provider files only according to the competition rules.

