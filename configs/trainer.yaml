# Trainer-only config; configs/base.yaml remains the full pipeline proposal.
training:
  seed: 42
  lr: 0.001
  weight_decay: 0.0001
  batch_size: 512
  max_epochs: 50
  early_stopping_patience: 5
  min_delta: 0.0
  huber_delta: 1.0
  device: cpu
